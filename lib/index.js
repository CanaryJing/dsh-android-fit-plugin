/**
 * @dsh-external/android-fit-plugin — host half (intentionally minimal).
 * This plugin is client-only: it adapts the settings panel for Android
 * (safe areas, viewport clamps, touch targets) and registers the
 * "安卓适配" settings section. No host-side services are required.
 */

export const name = '@dsh-external/android-fit-plugin'

export function apply() {
  // no-op host half
}
